% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jointRPCA.R
\name{jointRPCA}
\alias{jointRPCA}
\title{Joint Robust PCA on Multiple Compositional Tables}
\usage{
jointRPCA(
  tables,
  n.test.samples = 10,
  sample.metadata = NULL,
  train.test.column = NULL,
  n.components = 3,
  rclr.transform.tables = TRUE,
  min.sample.count = 0,
  min.feature.count = 0,
  min.feature.frequency = 0,
  max.iterations = 5
)
}
\arguments{
\item{tables}{A list of compositional data tables (matrices or data frames).
For automatic handling of Bioconductor objects (e.g., MultiAssayExperiment,
TreeSummarizedExperiment, SummarizedExperiment), or single matrices,
consider using \code{jointRPCAuniversal()} instead.}

\item{n.test.samples}{Integer specifying the number of samples to hold out for testing (only used if \code{sample.metadata} is NULL). Default is 10.}

\item{sample.metadata}{Optional data frame containing sample-level metadata. Must include a column indicating train/test labels.}

\item{train.test.column}{The name of the column in \code{sample.metadata} that defines training vs test samples.}

\item{n.components}{Integer specifying the number of principal components to compute. Must be at least 2.}

\item{rclr.transform.tables}{Logical; whether to apply rclr transformation to each input table before ordination. Default is TRUE.}

\item{min.sample.count}{Minimum total count required for a sample to be retained during filtering. Default is 0 (no filtering).}

\item{min.feature.count}{Minimum total count required for a feature to be retained. Default is 0 (no filtering).}

\item{min.feature.frequency}{Minimum percentage (0–100) of samples in which a feature must be non-zero to be retained. Default is 0.}

\item{max.iterations}{Maximum number of optimization iterations. Must be at least 1.}
}
\value{
A list with:
\describe{
\item{ord.res}{An \code{OrdinationResults} object containing sample scores, feature loadings, and variance explained.}
\item{U.dist.res}{A distance matrix (samples × samples) based on the learned sample embeddings.}
\item{cv.dist}{A data frame of cross-validation error statistics across iterations.}
\item{rclr.tables}{A list of CLR-transformed tables used as input for the ordination. These are useful for accessing dataset-specific embeddings or for reuse in downstream analyses.}
}
}
\description{
Performs Joint Robust Principal Component Analysis (RPCA) using OptSpace on multiple compositional tables.
Automatically handles shared sample alignment, optional train/test splitting, and compositional preprocessing.
}
\examples{
\dontrun{
# See examples/joint_rpca_example.qmd for a full reproducible test case.
}

}
