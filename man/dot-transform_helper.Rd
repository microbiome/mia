% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jointRPCA.R
\name{.transform_helper}
\alias{.transform_helper}
\title{Project New Data into Existing Ordination Space}
\usage{
.transform_helper(Udf, Vdf, s.eig, table.rclr.project, dedup.samples = TRUE)
}
\arguments{
\item{Udf}{Matrix of training sample embeddings (samples × components).}

\item{Vdf}{Matrix or list of feature loadings (features × components, or per-view list).}

\item{s.eig}{Singular values from the RPCA decomposition.}

\item{table.rclr.project}{New rCLR-transformed table(s) for projection (features × samples).
If \code{Vdf} is a matrix, provide a single matrix; if \code{Vdf} is a list, provide a named list of matrices per view.}

\item{dedup.samples}{Logical; whether to merge samples with identical names (e.g. suffixes like \verb{_1}, \verb{_2})
by averaging their feature values. Default is \code{TRUE}. Set to \code{FALSE} to preserve all duplicate sample IDs.}
}
\value{
A combined matrix of training and projected samples (samples × components).
}
\description{
Internal function to align rCLR-transformed samples to an existing RPCA ordination space.
Handles feature alignment, deduplication of sample names, double-centering normalization,
and projection into low-rank space using previously learned components.
}
\keyword{internal}
