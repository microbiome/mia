% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/filterRPCAInput.R
\name{filterRPCAInput}
\alias{filterRPCAInput}
\alias{filterRPCAInput,SummarizedExperiment-method}
\title{Filter raw count assays before RPCA or Joint-RPCA}
\usage{
filterRPCAInput(x, ...)

\S4method{filterRPCAInput}{SummarizedExperiment}(
  x,
  assay.type = "counts",
  min.sample.count = 0,
  min.feature.count = 0,
  min.feature.frequency = 0,
  ...
)
}
\arguments{
\item{x}{a \code{\link[SummarizedExperiment]{SummarizedExperiment}} object.}

\item{...}{additional arguments.}

\item{assay.type}{\code{Character scalar}. Specifies the name of assay
used in calculation. (Default: \code{"counts"})}

\item{min.sample.count}{\code{Numeric scalar} or \code{NULL}. Specifies the
minimum sample total count. Samples with total counts less than or equal to
this value are removed. If \code{NULL}, this filter is skipped.
(Default: \code{0})}

\item{min.feature.count}{\code{Numeric scalar} or \code{NULL}. Specifies the
minimum feature total count. Features with total counts less than or equal
to this value are removed. If \code{NULL}, this filter is skipped.
(Default: \code{0})}

\item{min.feature.frequency}{\code{Numeric scalar} or \code{NULL}. Specifies
the minimum feature prevalence across samples as a proportion between 0 and
\enumerate{
\item Features with prevalence less than or equal to this value are removed.
If \code{NULL}, this filter is skipped. (Default: \code{0})
}}
}
\value{
Filtered \code{SummarizedExperiment} object.
}
\description{
\code{filterRPCAInput()} applies Gemelli-style preprocessing filters to raw
count assays before robust centered log-ratio transformation and RPCA.

The filters are intended for raw count data, not transformed assays such as
rclr. Samples are filtered by total count. Features are filtered by total
count and prevalence across samples.
}
\details{
This function mirrors Gemelli-style RPCA table preprocessing:
\itemize{
\item sample total count must be \verb{> min.sample.count}
\item feature total count must be \verb{> min.feature.count}
\item feature prevalence percentage must be \verb{> min.feature.frequency}
}

The strict \code{>} comparison is intentional.

For exact comparison with the current Gemelli script, use:

\if{html}{\out{<div class="sourceCode">}}\preformatted{min.sample.count = 0
min.feature.count = 0
min.feature.frequency = 0
}\if{html}{\out{</div>}}
}
\examples{

data(GlobalPatterns)
tse <- GlobalPatterns

tse_sub <- filterRPCAInput(
    tse,
    min.sample.count = 5,
    min.feature.count = 10,
    min.feature.frequency = 0.3
)

}
\references{
Martino, C. and Shenhav, L. et al. (2020)
Context-aware dimensionality reduction deconvolutes gut microbial community
dynamics.
\emph{Nat. Biotechnol.} doi:10.1038/s41587-020-0660-7
}
\seealso{
\code{\link[=addRPCA]{addRPCA}} and
\code{\link[=addJointRPCA]{addJointRPCA}}
}
