% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filterRPCAInput.R
\name{filterRPCAInput}
\alias{filterRPCAInput}
\title{Filter raw count assays before RPCA or Joint-RPCA}
\usage{
filterRPCAInput(
  x,
  assay.type = "counts",
  experiments = NULL,
  assay.types = NULL,
  min.sample.count = 0,
  min.feature.count = 0,
  min.feature.frequency = 0
)
}
\arguments{
\item{x}{A \code{SummarizedExperiment} or \code{MultiAssayExperiment} object.}

\item{assay.type}{Character scalar. Assay name to filter when \code{x} is a
\code{SummarizedExperiment}.}

\item{experiments}{Character or integer vector. Experiments to filter when
\code{x} is a \code{MultiAssayExperiment}.}

\item{assay.types}{Character vector. Assay names corresponding to
\code{experiments} when \code{x} is a \code{MultiAssayExperiment}.}

\item{min.sample.count}{Numeric scalar or \code{NULL}. Samples are retained when
their total count is strictly greater than this value. If \code{NULL}, this
filter is skipped.}

\item{min.feature.count}{Numeric scalar or \code{NULL}. Features are retained when
their total count is strictly greater than this value. If \code{NULL}, this
filter is skipped.}

\item{min.feature.frequency}{Numeric scalar or \code{NULL}. Features are retained
when their percentage prevalence across samples is strictly greater than
this value. If \code{NULL}, this filter is skipped.}
}
\value{
The filtered object.
}
\description{
\code{filterRPCAInput()} applies Gemelli-style preprocessing filters to raw count
assays before robust centered log-ratio transformation and RPCA.

The filters are intended for raw count data, not transformed assays such as
\code{rclr}. Samples are filtered by total count. Features are filtered by total
count and prevalence across samples.
}
\details{
This helper mirrors Gemelli-style RPCA table preprocessing:
\itemize{
\item sample total count must be \verb{> min.sample.count}
\item feature total count must be \verb{> min.feature.count}
\item feature prevalence percentage must be \verb{> min.feature.frequency}
}

The strict \code{>} comparison is intentional.

For exact comparison with the current Gemelli script, use:

\if{html}{\out{<div class="sourceCode">}}\preformatted{min.sample.count = 0
min.feature.count = 0
min.feature.frequency = 0
}\if{html}{\out{</div>}}
}
